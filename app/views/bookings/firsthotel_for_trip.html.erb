<div class="text-gray-200 pt-2 pb-2 mt-8 p-4 text-center">
  <h4>Add a direction</h4>
  <p>Adding the direction of where you will be staying will allow you to search activities around ! </p></br>
  <p class="italic text-xs">If you don't know yet the exact direction, you can just put the city name. </p>
</div>

<div class="p-4 rounded-lg text-gray-400 text-center m-6 left-11 border-2 rounded-xl my-auto">
  <%= form_for([@trip, @booking]) do |f| %>
    <!-- START Hidden fields to retrieve info transparently from Trip in creation -->
    <%= f.text_field :category, value: "Hotel", hidden: true %>
    <%= f.date_field :begin_date, value: @trip.begin_date.to_date, hidden: true %>
    <%= f.date_field :end_date, value: @trip.end_date.to_date, hidden: true %>
    <!-- END Hidden fields to retrieve info transparently from Trip in creation -->
    <div class="field p-4">
      <%= f.label "Stay name" %>
      <%= f.text_field :name %>
    </div>
    <div class="field p-4">
      <%= f.label "Stay address" %>
      <%= f.text_field :address %>
    </div>
    <div>
      <%= f.submit "Add", class: "text-gray-200 text-md font-bold my-3 bg-transparent" %>
    </div>
  <% end %>
</div>
