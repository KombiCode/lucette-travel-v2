<!-- Hidden explanation that we are going to show only if there is no trip created -->
<% if @trip.nil? || @trip.id.nil? %>
  <%= render 'shared/explanation'%>
  <%= render 'shared/newtrip_form'%>
<% else %>
  <%= render 'shared/newtrip_form'%>
<% end %>


<!-- JS ADD EVENT LISTENER pour display none  -->
<!-- AJOUTER LIGNE DE CODE : display non sur id à la fin  -->
